What would this print to the terminal:

```javascript
var isFriday = function(day){
     var d = new Date();
     var day = d.getDay();
     var isFriday = day === 5;
     console.log(isFriday);
}

console.log(isFriday(true));

```
